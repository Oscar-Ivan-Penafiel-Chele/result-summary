<section>
    <p class="title__summary__right">Your Result</p>

    <article>
        <p>76 <span>of 100</span></p>
    </article>

    <h2>Great</h2>

    <p>You scored higher than 65% of the people who have taken these tests.</p>
</section>

<style>
    section{
        background: linear-gradient(var(--light-slate-blue), var(--light-royal-blue ));
        max-width: 350px;
        width: 100%;
        height: 100%;
        margin-inline: auto;
        border-radius: 1.3rem;
        display: flex;
        flex-flow: column nowrap;
        justify-content: center;
        align-items: center;
        gap: 1.3rem;
        padding: 4rem;
    }

    article{
        background: linear-gradient(var(--violet-blue), var(--persian-blue));
        border-radius: 50%;
        padding: 3rem 4rem;
        border: none
    }

    article > p{
        display: flex;
        flex-flow: column nowrap;
        justify-content: center;
        align-items: center;
        color: var(--white);
        font-family: HankenGrotesk-extrabold;
        font-size: 4rem;
    }

    article > p > span{
        color: var(--gray);
        font-size: 1.2rem;
        font-family: HankenGrotesk-bold;
    }

    h2{
        color: var(--white);
        font-family: HankenGrotesk-variable;
        font-size: 2rem;
    }

    section > p:last-child{
        font-size: 1.08rem;
        font-family: HankenGrotesk-variable;
        color: var(--purple);
        width: 100%;
        text-align: center;
    }

    .title__summary__right{
        font-size: 1.4em;
        font-family: HankenGrotesk-bold;
        color: var(--purple);
        margin-botton: 1rem;
    }

    @media screen and (max-width: 45em){
        section{
            max-width: 100%;
            border-top-left-radius: 0px;
            border-top-right-radius: 0px;
            border-bottom-left-radius: 3rem;
            border-bottom-right-radius: 3rem;
            padding: 3rem 2.5rem;
        }

        article{
            padding: 2rem 3rem;
        }
   }
</style>